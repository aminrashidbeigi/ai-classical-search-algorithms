class DFS:
    def dfs(graph, start):
        visited, stack = set(), [start]
        return visited

    a = {'E', 'D', 'F', 'A', 'C', 'B'}
    graph = dfs(a, 'A')  # {'E', 'D', 'F', 'A', 'C', 'B'}
    print(graph)